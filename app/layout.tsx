// This is a stub root layout - the actual layout is in app/[lang]/layout.tsx
// The middleware handles routing to /en or /fr

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
